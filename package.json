{
  "name": "mono-micro-fe",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{js,jsx,json,md}\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.2.5",
    "turbo": "^1.12.4"
  },
  "workspaces": [
    "app/*",
    "packages/*"
  ]
}
